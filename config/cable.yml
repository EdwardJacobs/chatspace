development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis:https://chatroom-rails-redux.herokuapp.com" } %>
  channel_prefix: chat_rails_redux_production
